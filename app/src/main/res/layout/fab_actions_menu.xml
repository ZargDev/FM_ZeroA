<!-- res/layout/fab_actions_menu.xml -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Overlay transparente que cubre TODO y sirve para cerrar -->
    <View
        android:id="@+id/overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone"
        android:background="@android:color/transparent"
        android:clickable="true"/>

    <!-- Fab principal -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_main"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        app:fabCustomSize="64dp"
        app:maxImageSize="32dp"
        app:srcCompat="@drawable/ic_add"
        app:tint="@android:color/white"
        app:backgroundTint="@color/banana_primary"/>

    <!-- Fab carpeta -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_create_folder"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        app:fabCustomSize="56dp"
        app:maxImageSize="24dp"
        app:srcCompat="@drawable/ic_create_new_folder"
        app:tint="@android:color/white"
        app:backgroundTint="@color/banana_secondary"/>

    <!-- Fab .txt -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_create_txt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        app:fabCustomSize="56dp"
        app:maxImageSize="24dp"
        app:srcCompat="@drawable/ic_insert_drive_file"
        app:tint="@android:color/white"
        app:backgroundTint="@color/banana_secondary"/>

</FrameLayout>
